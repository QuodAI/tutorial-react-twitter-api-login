[build]
  publish = "react/build"
  command = "cd react && npm install && npm run build && cd ../express && npm install && npm run build"
  functions = "express/functions"
[build.environment]
  SERVERLESS = "1"